{
  "name": "mcp-hono",
  "version": "1.3.0",
  "description": "SISPROT MCP Server - Hono + Bun Edition (Railway)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "type-check": "bunx tsc --noEmit",
    "test": "bunx vitest run",
    "test:watch": "bunx vitest"
  },
  "dependencies": {
    "@ai-sdk/google": "^1.2.3",
    "@google/generative-ai": "^0.24.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "hono": "^4.7.2",
    "postgres": "^3.4.8",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}
